x-app: &app
  build:
    context: .
  image: avito-kitchen:local
  environment: &app-env
    APP_ENV: dev
    LOG_LEVEL: info

services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: kitchen
      POSTGRES_PASSWORD: kitchen
      POSTGRES_DB: kitchen
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -h 127.0.0.1 -U kitchen -d kitchen"]
      interval: 10s
      timeout: 5s
      retries: 5

  redpanda:
    image: redpandadata/redpanda:v24.3.6
    command:
      - redpanda
      - start
      - --mode=dev-container
      - --smp=1
      - --kafka-addr=PLAINTEXT://0.0.0.0:9092
      - --advertise-kafka-addr=PLAINTEXT://redpanda:9092
    healthcheck:
      test: ["CMD-SHELL", "rpk cluster health | grep -E 'Healthy:.+true'"]
      interval: 5s
      timeout: 5s
      retries: 20

  redpanda-console:
    image: redpandadata/console:v2.8.5
    environment:
      KAFKA_BROKERS: redpanda:9092
    ports:
      - "8080:8080"
    depends_on:
      redpanda:
        condition: service_healthy

  migrator:
    image: migrate/migrate:v4.19.1
    profiles: [setup]
    restart: "no"
    volumes:
      - ./migrations/kitchen:/migrations
    entrypoint:
      - migrate
      - -path=/migrations
      - -database=postgres://kitchen:kitchen@postgres:5432/kitchen?sslmode=disable
    command: ["up"]
    depends_on:
      postgres:
        condition: service_healthy

  seeder:
    <<: *app
    command: ["/app/seed"]
    profiles: [setup]
    restart: "no"
    environment:
      <<: *app-env
      POSTGRES_DSN: postgres://kitchen:kitchen@postgres:5432/kitchen?sslmode=disable
    depends_on:
      migrator:
        condition: service_completed_successfully

  kitchen-api:
    <<: *app
    command: ["/app/kitchen-api"]
    environment:
      <<: *app-env
      HTTP_ADDR: ":8081"
      POSTGRES_DSN: postgres://kitchen:kitchen@postgres:5432/kitchen?sslmode=disable
    ports:
      - "8081:8081"
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O - http://127.0.0.1:8081/healthz || exit 1"]
      interval: 3s
      timeout: 3s
      retries: 10
    depends_on:
      postgres:
        condition: service_healthy
      redpanda:
        condition: service_healthy

  worker:
    <<: *app
    command: ["/app/worker"]
    environment:
      <<: *app-env
      HTTP_ADDR: ":8080"
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O - http://127.0.0.1:8080/healthz || exit 1"]
      interval: 3s
      timeout: 3s
      retries: 10
    depends_on:
      postgres:
        condition: service_healthy
      redpanda:
        condition: service_healthy

  # The demo venue: a stand-in for a partner's own system, not a part of the
  # platform. It talks to kitchen-api the way any restaurant would.
  venue-service:
    <<: *app
    command: ["/app/venue-service"]
    environment:
      <<: *app-env
      HTTP_ADDR: ":8082"
    ports:
      - "8082:8082"
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O - http://127.0.0.1:8082/healthz || exit 1"]
      interval: 3s
      timeout: 3s
      retries: 10
    depends_on:
      kitchen-api:
        condition: service_healthy
      redpanda:
        condition: service_healthy

volumes:
  pgdata:
